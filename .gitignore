# Build output
backend/target/
frontend/node_modules/
frontend/dist/

# IDE / OS
.idea/
*.iml
.vscode/
.DS_Store

# Local env — .env.* variants can hold real credentials (e.g. .env.prod with the
# Render database password); only the checked-in .env.example is allowed through.
.env
.env.*
!.env.example
*.local

# Assessment brief — kept out of the published repo on purpose
test.md
