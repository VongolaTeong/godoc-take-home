# Only needed when the UI is deployed on a different origin than the API
# (e.g. Cloudflare Pages -> Render). Leave unset for same-origin setups.
VITE_API_BASE_URL=https://your-api-service.onrender.com
